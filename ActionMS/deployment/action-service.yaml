apiVersion: v1
kind: Service
metadata:
  labels:
    app: action-app
  name: action-service
spec:
  ports:
  - port: 8082
    protocol: TCP
    targetPort: 8082
  selector:
    app: action-app
  type: NodePort
status:
  loadBalancer: {}
