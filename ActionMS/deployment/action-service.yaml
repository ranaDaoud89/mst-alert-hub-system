apiVersion: v1
kind: Service
metadata:
  labels:
    app: action-app
  name: action-service
spec:
  ports:
  - port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    app: action-app
  type: NodePort
status:
  loadBalancer: {}
